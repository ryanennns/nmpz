## Code Style

- use `->getKey()`, not `->id`
- never omit `::query()->` when making a model query
- use tailwindcss
- before saying any front end change is completed, run prettier, eslint, and vitest
